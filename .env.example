# Deployer wallet private key (NEVER commit the actual .env file!)
PRIVATE_KEY=0x...

# Filecoin Calibration RPC URL
CALIBRATION_RPC=https://api.calibration.node.glif.io/rpc/v1

# Frontend environment variable (set after deployment)
VITE_CONTRACT_ADDRESS=0x...
